<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mongoose Store</title>
</head>
<body>
    <h1>Products</h1>
    <div>
         <ul>
             <% for(let i = 0; i < products.length; i++) { %>
                <li>
                    <a href='/products/<%= products[i]._id %>'>
                        <img src="<%= products[i].img%>" width="200" height="150">
                    </a>
                </li>
             <% } %>
         </ul>
    </div>
    <a href='/newproduct'>Create a new product</a>
</body>
</html>